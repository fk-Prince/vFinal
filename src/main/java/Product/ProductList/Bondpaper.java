package Product.ProductList;

import Product.Entity.Product;

public class Bondpaper extends Product {

    public Bondpaper(String productType, int productId, String brandName, int productQty, double productPrice) {
        super(productType, productId, brandName, productQty, productPrice);
    }
}
