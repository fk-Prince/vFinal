package Product.ProductList;

import Product.Entity.Product;

public class Crayons extends Product {

    public Crayons(String productType, int productId, String brandName, int productQty, double productPrice) {
        super(productType, productId, brandName, productQty, productPrice);
    }
}
